# ローカル開発用の環境変数。コピーして .env を作る(.env は Git 管理外)。
APP_ENV=development
PORT=8080
# ローカル Postgres(deploy/docker/docker-compose.yml、ホスト側ポート 5433)
DATABASE_URL=postgres://dispatch:dispatch@localhost:5433/dispatch?sslmode=disable
